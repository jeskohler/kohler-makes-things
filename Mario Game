int main (void)
{
int height;
do 
{
     height = get_int("Height: ")
}
  while(height < 1 || height > 8);        
  
  printf("%i", name); 
  printf("\n\n");
{
  for(int column=0; column < 3; firstCounter++) 
  {
    for (int secondCOunter=0; secondCounter < 3; column++)
    {
      printf("row %i column %i", row, column);
    }
  }
}
